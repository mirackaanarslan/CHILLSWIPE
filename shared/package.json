{
  "name": "@prediction-market/shared",
  "version": "1.0.0",
  "description": "Shared types and utilities for prediction market app",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@types/node": "^24.0.11",
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "firebase": "^10.5.0",
    "react-hot-toast": "^2.5.2"
  }
}
